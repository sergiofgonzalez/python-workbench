# Basic of prompt engineering: using clear instructions

## Program description

The program starts by loading all the JSON files in the `prompts/` directory. Then, it displays the list of files as numeric choices and allows the user to choose an option. After the selection is done, the prompts are submitted to the LLM and the response is printed.