# Basic asyncio programming in Python

## Setting up shop

The project was created using:

```bash
$ uv init 01_hello-asyncio
```

The [`pyproject.toml`](pyproject.toml) was customized to use [Ruff](https://docs.astral.sh/ruff) linter.