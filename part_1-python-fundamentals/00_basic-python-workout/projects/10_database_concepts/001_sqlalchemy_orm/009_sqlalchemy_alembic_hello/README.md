# 009: Hello, Alembic DB migrations
> Illustrates the basics of Alembic for DB migrations

## Project description

ToDo

### TODO: illustrate what to do to develop some intuition on the concepts

ToDo

## Running the program

You can run the application with:

```bash
uv run main.py
```

## Project management

This project is managed using `uv`.

FastAPI dependency was added using:

```bash
$ uv add SQLAlchemy
```

as I don't intend to use FastAPI cloud at the moment.

The only other dependency was ruff:

```bash
$ uv add ruff --dev
```
