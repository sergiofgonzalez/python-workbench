# 04: fixtures: additional features
> illustrates additional features of fixtures

See [README.md](../README.md#04-pytest-more-on-fixtures) for full details.

You can run the application with:

```bash
uv run main.py
```

And you can run your tests from the command line with:

```bash
uv run pytest
```
