# 03: fixtures for passing values
> illustrates a more advanced usage of fixtures that passes data to the tests

See [README.md](../README.md#03-pytest-fixtures-for-passing-data-to-the-tests) for full details.

You can run the application with:

```bash
uv run main.py
```

And you can run your tests from the command line with:

```bash
uv run pytest
```
