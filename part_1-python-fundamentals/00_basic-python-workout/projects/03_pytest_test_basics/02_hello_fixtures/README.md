# 02: hello, fixtures
> basics of pytest fixtures: setup/tear-down fixture that announce itself


See [README.md](../README.md#02-pytest-fixtures-for-setup-and-teardown) for full details.

You can run the application with:

```bash
uv run main.py
```

And you can run your tests from the command line with:

```bash
# run pytest without capturing print statements
uv run pytest -s
```
