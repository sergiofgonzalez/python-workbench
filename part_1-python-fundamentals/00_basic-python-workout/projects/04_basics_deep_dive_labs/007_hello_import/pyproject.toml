[project]
name = "007_hello_imports"
version = "0.1.0"
description = "Lab 007: Hello imports illustrate basic import usage."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["ruff>=0.14.8"]

[tool.ruff]

ignore = [
  "T201", # Allow print statements
]

# Enable all rules
select = ["ALL"]

[tool.ruff.lint.pydocstyle]
convention = "google"
